<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Koki AI Hub</title>
    
    <!-- PWA 설정 -->
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="theme-color" content="#667eea">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            height: 100vh;
            overflow: hidden;
            max-width: 414px;
            margin: 0 auto;
            position: relative;
            box-shadow: 0 0 50px rgba(0,0,0,0.3);
        }

        .status-bar {
            background: rgba(0,0,0,0.8);
            height: 44px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
            font-size: 14px;
            font-weight: 600;
        }

        .header {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        .status-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: #ff4757;
            animation: pulse 2s infinite;
        }

        .status-dot.connected {
            background: #2ed573;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        .main-container {
            height: calc(100vh - 200px);
            overflow-y: auto;
            padding: 20px;
            -webkit-overflow-scrolling: touch;
        }

        .page {
            display: none;
            animation: slideIn 0.3s ease;
        }

        .page.active {
            display: block;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateX(20px); }
            to { opacity: 1; transform: translateX(0); }
        }

        .chat-container {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .messages {
            flex: 1;
            overflow-y: auto;
            margin-bottom: 15px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .message {
            max-width: 85%;
            padding: 12px 16px;
            border-radius: 18px;
            animation: messageSlide 0.3s ease;
        }

        .message.user {
            background: linear-gradient(45deg, #667eea, #764ba2);
            align-self: flex-end;
            border-bottom-right-radius: 6px;
        }

        .message.assistant {
            background: rgba(255, 255, 255, 0.2);
            align-self: flex-start;
            border-bottom-left-radius: 6px;
        }

        @keyframes messageSlide {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .input-area {
            display: flex;
            gap: 10px;
            align-items: flex-end;
        }

        .message-input {
            flex: 1;
            background: rgba(255, 255, 255, 0.15);
            border: none;
            border-radius: 20px;
            padding: 12px 16px;
            color: white;
            font-size: 16px;
            resize: none;
            min-height: 44px;
            max-height: 100px;
        }

        .message-input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .btn {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            border: none;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .send-btn {
            background: linear-gradient(45deg, #2ed573, #1abc9c);
        }

        .voice-btn {
            background: linear-gradient(45deg, #ffa502, #ff6348);
        }

        .voice-btn.recording {
            background: linear-gradient(45deg, #ff4757, #ff3742);
            animation: recordPulse 1s infinite;
        }

        @keyframes recordPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        .btn:active {
            transform: scale(0.95);
        }

        .settings-page {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px;
            height: 100%;
            overflow-y: auto;
        }

        .setting-item {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 15px;
        }

        .setting-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .api-input {
            width: 100%;
            background: rgba(255, 255, 255, 0.15);
            border: none;
            border-radius: 12px;
            padding: 12px 16px;
            color: white;
            font-size: 16px;
            margin-bottom: 10px;
        }

        .api-input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .connect-btn {
            width: 100%;
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 12px;
            padding: 14px;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .connect-btn:active {
            transform: scale(0.98);
        }

        .connect-btn.connected {
            background: linear-gradient(45deg, #2ed573, #1abc9c);
        }

        .dual-ai-page {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .ai-tabs {
            display: flex;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 4px;
            margin-bottom: 20px;
        }

        .ai-tab {
            flex: 1;
            background: transparent;
            border: none;
            color: white;
            padding: 10px;
            border-radius: 8px;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .ai-tab.active {
            background: rgba(255, 255, 255, 0.2);
        }

        .question-input {
            width: 100%;
            background: rgba(255, 255, 255, 0.15);
            border: none;
            border-radius: 15px;
            padding: 15px;
            color: white;
            font-size: 16px;
            min-height: 100px;
            resize: none;
            margin-bottom: 15px;
        }

        .process-btn {
            width: 100%;
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 12px;
            padding: 15px;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            margin-bottom: 20px;
        }

        .ai-response {
            flex: 1;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 15px;
            padding: 15px;
            white-space: pre-wrap;
            line-height: 1.5;
            font-size: 0.9rem;
            overflow-y: auto;
        }

        .video-page {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px;
            text-align: center;
            height: 100%;
        }

        .video-area {
            background: rgba(0, 0, 0, 0.4);
            border-radius: 15px;
            height: 200px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .video-controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
        }

        .video-btn {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: none;
            color: white;
            font-size: 1.3rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .video-btn.camera {
            background: linear-gradient(45deg, #667eea, #764ba2);
        }

        .video-btn.mic {
            background: linear-gradient(45deg, #2ed573, #1abc9c);
        }

        .video-btn.end {
            background: linear-gradient(45deg, #ff4757, #ff3742);
        }

        .video-btn.active {
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
        }

        .tools-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .tool-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .tool-card:active {
            transform: scale(0.95);
            background: rgba(255, 255, 255, 0.2);
        }

        .tool-icon {
            font-size: 2rem;
            margin-bottom: 8px;
            display: block;
        }

        .tool-name {
            font-size: 0.9rem;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .tool-desc {
            font-size: 0.75rem;
            opacity: 0.8;
        }

        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            max-width: 414px;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(20px);
            padding: 10px 20px 25px 20px;
            display: flex;
            justify-content: space-around;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
        }

        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 4px;
            padding: 8px 12px;
            border-radius: 12px;
            transition: all 0.3s ease;
            cursor: pointer;
            min-width: 60px;
        }

        .nav-item.active {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.05);
        }

        .nav-icon {
            font-size: 1.2rem;
        }

        .nav-text {
            font-size: 0.7rem;
            font-weight: 500;
        }

        .notification {
            position: fixed;
            top: 60px;
            left: 20px;
            right: 20px;
            background: linear-gradient(45deg, #2ed573, #1abc9c);
            color: white;
            padding: 15px 20px;
            border-radius: 15px;
            transform: translateY(-100px);
            transition: transform 0.3s ease;
            z-index: 1000;
            text-align: center;
            font-weight: 500;
            max-width: 374px;
            margin: 0 auto;
        }

        .notification.show {
            transform: translateY(0);
        }

        .notification.error {
            background: linear-gradient(45deg, #ff4757, #ff3742);
        }

        .notification.warning {
            background: linear-gradient(45deg, #ffa502, #ff6348);
        }

        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 50px;
            height: 24px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255,255,255,0.3);
            border-radius: 24px;
            transition: 0.3s;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 18px;
            width: 18px;
            left: 3px;
            bottom: 3px;
            background: white;
            border-radius: 50%;
            transition: 0.3s;
        }

        input:checked + .slider {
            background: linear-gradient(45deg, #2ed573, #1abc9c);
        }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

        ::-webkit-scrollbar {
            display: none;
        }

        @media (min-width: 415px) {
            body {
                margin: 20px auto;
                border-radius: 30px;
                overflow: hidden;
                box-shadow: 0 20px 60px rgba(0,0,0,0.5);
            }
        }
    </style>
</head>
<body>
    <!-- 상태바 -->
    <div class="status-bar">
        <div>
            <span id="currentTime">9:41</span>
        </div>
        <div style="display: flex; align-items: center; gap: 5px;">
            <span>📶</span>
            <span>🔋</span>
            <span style="font-size: 12px;">100%</span>
        </div>
    </div>

    <!-- 헤더 -->
    <div class="header">
        <h1>🤖 Koki AI</h1>
        <div style="display: flex; align-items: center; gap: 8px;">
            <span style="font-size: 0.8rem;">API</span>
            <div class="status-dot" id="statusDot"></div>
        </div>
    </div>

    <!-- 메인 컨테이너 -->
    <div class="main-container">
        <!-- 채팅 페이지 -->
        <div class="page active" id="chatPage">
            <div class="chat-container">
                <div class="messages" id="messages">
                    <div class="message assistant">
                        안녕하세요! 저는 Koki입니다. 무엇을 도와드릴까요? 🤖
                    </div>
                </div>
                <div class="input-area">
                    <textarea class="message-input" id="messageInput" placeholder="메시지를 입력하세요..." rows="1"></textarea>
                    <button class="btn voice-btn" id="voiceBtn">🎤</button>
                    <button class="btn send-btn" id="sendBtn">📤</button>
                </div>
            </div>
        </div>

        <!-- 듀얼 AI 페이지 -->
        <div class="page" id="dualAiPage">
            <div class="dual-ai-page">
                <h2 style="margin-bottom: 20px; text-align: center;">🤖 Claude vs GPT-4</h2>
                
                <div class="ai-tabs">
                    <button class="ai-tab active" data-tab="claude">Claude</button>
                    <button class="ai-tab" data-tab="gpt">GPT-4</button>
                    <button class="ai-tab" data-tab="integrated">통합</button>
                </div>

                <textarea class="question-input" id="dualQuestion" placeholder="Claude와 GPT-4에게 질문하세요..."></textarea>
                
                <button class="process-btn" id="processBtn">🚀 AI 응답 생성</button>
                
                <div class="ai-response" id="dualResponse">
                    질문을 입력하고 '🚀 AI 응답 생성' 버튼을 눌러주세요.
                </div>
            </div>
        </div>

        <!-- 비디오 페이지 -->
        <div class="page" id="videoPage">
            <div class="video-page">
                <h2 style="margin-bottom: 20px;">📹 영상 채팅</h2>
                
                <div class="video-area" id="videoArea">
                    <div class="video-placeholder">
                        <div style="font-size: 3rem; margin-bottom: 10px;">🤖</div>
                        <div>카메라를 활성화하세요</div>
                    </div>
                    <video id="localVideo" style="width: 100%; height: 100%; object-fit: cover; border-radius: 15px; display: none;" autoplay muted></video>
                </div>

                <div class="video-controls">
                    <button class="video-btn camera" id="cameraBtn">📹</button>
                    <button class="video-btn mic" id="micBtn">🎤</button>
                    <button class="video-btn end" id="endBtn">📞</button>
                </div>

                <div style="margin-top: 20px; padding: 15px; background: rgba(255,255,255,0.1); border-radius: 15px;">
                    <h3 style="margin-bottom: 10px;">💬 음성 채팅</h3>
                    <div id="voiceMessages" style="min-height: 100px;">
                        <div class="message assistant">음성으로 대화해보세요!</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 도구 페이지 -->
        <div class="page" id="toolsPage">
            <h2 style="margin-bottom: 20px; text-align: center;">🛠️ AI 도구</h2>
            <div class="tools-grid">
                <div class="tool-card" data-tool="writer">
                    <span class="tool-icon">✍️</span>
                    <div class="tool-name">문서 작성</div>
                    <div class="tool-desc">AI로 문서를 자동 작성</div>
                </div>
                <div class="tool-card" data-tool="translator">
                    <span class="tool-icon">🌐</span>
                    <div class="tool-name">번역기</div>
                    <div class="tool-desc">다양한 언어로 번역</div>
                </div>
                <div class="tool-card" data-tool="coder">
                    <span class="tool-icon">💻</span>
                    <div class="tool-name">코드 생성</div>
                    <div class="tool-desc">AI로 코드 작성</div>
                </div>
                <div class="tool-card" data-tool="analyzer">
                    <span class="tool-icon">📊</span>
                    <div class="tool-name">분석기</div>
                    <div class="tool-desc">데이터 분석 도구</div>
                </div>
                <div class="tool-card" data-tool="summarizer">
                    <span class="tool-icon">📝</span>
                    <div class="tool-name">요약기</div>
                    <div class="tool-desc">긴 글을 요약</div>
                </div>
                <div class="tool-card" data-tool="tutor">
                    <span class="tool-icon">🎓</span>
                    <div class="tool-name">학습 도우미</div>
                    <div class="tool-desc">AI 개인 교사</div>
                </div>
            </div>
        </div>

        <!-- 설정 페이지 -->
        <div class="page" id="settingsPage">
            <div class="settings-page">
                <div class="setting-item">
                    <div class="setting-title">🔑 Claude API 설정</div>
                    <input type="password" class="api-input" id="claudeApiKey" placeholder="Claude API Key 입력">
                    <button class="connect-btn" id="connectBtn">연결하기</button>
                </div>

                <div class="setting-item">
                    <div class="setting-title">🎤 음성 설정</div>
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px;">
                        <span>음성 인식</span>
                        <label class="toggle-switch">
                            <input type="checkbox" id="speechToggle" checked>
                            <span class="slider"></span>
                        </label>
                    </div>
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <span>자동 읽기</span>
                        <label class="toggle-switch">
                            <input type="checkbox" id="ttsToggle" checked>
                            <span class="slider"></span>
                        </label>
                    </div>
                </div>

                <div class="setting-item">
                    <div class="setting-title">📱 앱 정보</div>
                    <div style="font-size: 0.9rem; opacity: 0.8; line-height: 1.6;">
                        <div>• 버전: 1.0.0</div>
                        <div>• 개발자: Koki AI Team</div>
                        <div>• 지원: Claude API</div>
                        <div>• 플랫폼: 모든 모바일 기기</div>
                    </div>
                </div>

                <div class="setting-item">
                    <div class="setting-title">🔄 데이터 관리</div>
                    <button class="connect-btn" style="background: linear-gradient(45deg, #ffa502, #ff6348);" onclick="clearCache()">
                        캐시 및 데이터 지우기
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- 하단 네비게이션 -->
    <div class="bottom-nav">
        <div class="nav-item active" data-page="chatPage">
            <span class="nav-icon">💬</span>
            <span class="nav-text">채팅</span>
        </div>
        <div class="nav-item" data-page="dualAiPage">
            <span class="nav-icon">🤖</span>
            <span class="nav-text">듀얼AI</span>
        </div>
        <div class="nav-item" data-page="videoPage">
            <span class="nav-icon">📹</span>
            <span class="nav-text">영상</span>
        </div>
        <div class="nav-item" data-page="toolsPage">
            <span class="nav-icon">🛠️</span>
            <span class="nav-text">도구</span>
        </div>
        <div class="nav-item" data-page="settingsPage">
            <span class="nav-icon">⚙️</span>
            <span class="nav-text">설정</span>
        </div>
    </div>

    <script>
        class KokiMobileApp {
            constructor() {
                this.apiKey = '';
                this.isConnected = false;
                this.currentPage = 'chatPage';
                this.recognition = null;
                this.synthesis = window.speechSynthesis;
                this.isRecording = false;
                this.localStream = null;
                this.isCameraOn = false;
                this.isMicOn = false;
                
                this.dualResponses = {
                    claude: '',
                    gpt: '',
                    integrated: ''
                };
                this.currentDualTab = 'claude';
                
                this.settings = {
                    speechEnabled: true,
                    ttsEnabled: true
                };
                
                this.init();
            }
            
            init() {
                this.setupEventListeners();
                this.initSpeechRecognition();
                this.loadSettings();
                this.loadApiKey();
                this.updateTime();
                
                setTimeout(() => {
                    this.showNotification('🤖 Koki AI 완전 활성화! 모든 기능 사용 가능');
                }, 1000);
            }
            
            setupEventListeners() {
                // 네비게이션
                document.querySelectorAll('.nav-item').forEach(item => {
                    item.addEventListener('click', (e) => {
                        const page = e.currentTarget.dataset.page;
                        this.switchPage(page);
                    });
                });
                
                // 채팅 기능
                document.getElementById('sendBtn').addEventListener('click', () => this.sendMessage());
                document.getElementById('voiceBtn').addEventListener('click', () => this.toggleVoiceRecording());
                document.getElementById('messageInput').addEventListener('keydown', (e) => {
                    if (e.key === 'Enter' && !e.shiftKey) {
                        e.preventDefault();
                        this.sendMessage();
                    }
                });
                
                // 입력창 자동 크기 조정
                document.getElementById('messageInput').addEventListener('input', (e) => {
                    e.target.style.height = 'auto';
                    e.target.style.height = Math.min(e.target.scrollHeight, 100) + 'px';
                });
                
                // API 연결
                document.getElementById('connectBtn').addEventListener('click', () => this.connectAPI());
                
                // 듀얼 AI 기능
                document.getElementById('processBtn').addEventListener('click', () => this.processDualAI());
                document.querySelectorAll('.ai-tab').forEach(tab => {
                    tab.addEventListener('click', (e) => this.switchDualTab(e.target.dataset.tab));
                });
                
                // 비디오 컨트롤
                document.getElementById('cameraBtn').addEventListener('click', () => this.toggleCamera());
                document.getElementById('micBtn').addEventListener('click', () => this.toggleMicrophone());
                document.getElementById('endBtn').addEventListener('click', () => this.endVideoCall());
                
                // 도구 카드
                document.querySelectorAll('.tool-card').forEach(card => {
                    card.addEventListener('click', (e) => {
                        const tool = e.currentTarget.dataset.tool;
                        this.openTool(tool);
                    });
                });
                
                // 설정 토글
                document.getElementById('speechToggle').addEventListener('change', (e) => {
                    this.settings.speechEnabled = e.target.checked;
                    this.saveSettings();
                    this.showNotification(e.target.checked ? '음성 인식 활성화 ✅' : '음성 인식 비활성화 ❌');
                });
                
                document.getElementById('ttsToggle').addEventListener('change', (e) => {
                    this.settings.ttsEnabled = e.target.checked;
                    this.saveSettings();
                    this.showNotification(e.target.checked ? '자동 읽기 활성화 ✅' : '자동 읽기 비활성화 ❌');
                });
            }
            
            switchPage(pageId) {
                document.querySelectorAll('.page').forEach(page => page.classList.remove('active'));
                document.querySelectorAll('.nav-item').forEach(item => item.classList.remove('active'));
                
                document.getElementById(pageId).classList.add('active');
                document.querySelector(`[data-page="${pageId}"]`).classList.add('active');
                
                this.currentPage = pageId;
                
                if (pageId === 'videoPage') {
                    this.initVideoPage();
                }
            }
            
            initSpeechRecognition() {
                if ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window) {
                    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
                    this.recognition = new SpeechRecognition();
                    this.recognition.continuous = false;
                    this.recognition.interimResults = false;
                    this.recognition.lang = 'ko-KR';
                    
                    this.recognition.onresult = (event) => {
                        const result = event.results[0][0].transcript;
                        if (this.currentPage === 'chatPage') {
                            document.getElementById('messageInput').value = result;
                            this.sendMessage();
                        } else if (this.currentPage === 'dualAiPage') {
                            document.getElementById('dualQuestion').value = result;
                        }
                        this.stopRecording();
                    };
                    
                    this.recognition.onerror = () => {
                        this.stopRecording();
                        this.showNotification('음성 인식 오류가 발생했습니다', 'error');
                    };
                    
                    this.recognition.onend = () => {
                        this.stopRecording();
                    };
                }
            }
            
            toggleVoiceRecording() {
                if (!this.settings.speechEnabled) {
                    this.showNotification('설정에서 음성 인식을 활성화하세요', 'warning');
                    return;
                }
                
                if (!this.recognition) {
                    this.showNotification('음성 인식이 지원되지 않습니다', 'error');
                    return;
                }
                
                if (this.isRecording) {
                    this.recognition.stop();
                } else {
                    this.startRecording();
                }
            }
            
            startRecording() {
                this.isRecording = true;
                document.getElementById('voiceBtn').classList.add('recording');
                document.getElementById('voiceBtn').textContent = '⏹️';
                
                try {
                    this.recognition.start();
                    this.showNotification('🎤 음성 인식 시작!');
                } catch (error) {
                    this.stopRecording();
                    this.showNotification('음성 인식을 시작할 수 없습니다', 'error');
                }
            }
            
            stopRecording() {
                this.isRecording = false;
                document.getElementById('voiceBtn').classList.remove('recording');
                document.getElementById('voiceBtn').textContent = '🎤';
            }
            
            async sendMessage() {
                const input = document.getElementById('messageInput');
                const message = input.value.trim();
                
                if (!message) return;
                
                this.addMessage(message, 'user');
                input.value = '';
                input.style.height = 'auto';
                
                const loadingId = this.addMessage('답변을 생성하고 있습니다...', 'assistant');
                
                try {
                    const response = await this.callKokiAPI(message);
                    this.removeMessage(loadingId);
                    this.addMessage(response, 'assistant');
                    
                    if (this.settings.ttsEnabled) {
                        this.speakText(response);
                    }
                } catch (error) {
                    this.removeMessage(loadingId);
                    this.addMessage('죄송합니다. 오류가 발생했습니다: ' + error.message, 'assistant');
                }
            }
            
            addMessage(text, sender) {
                const messagesContainer = document.getElementById('messages');
                const messageDiv = document.createElement('div');
                const messageId = 'msg_' + Date.now();
                
                messageDiv.className = `message ${sender}`;
                messageDiv.id = messageId;
                messageDiv.textContent = text;
                
                messagesContainer.appendChild(messageDiv);
                messagesContainer.scrollTop = messagesContainer.scrollHeight;
                
                return messageId;
            }
            
            removeMessage(messageId) {
                const message = document.getElementById(messageId);
                if (message) {
                    message.remove();
                }
            }
            
            async connectAPI() {
                const apiKey = document.getElementById('claudeApiKey').value.trim();
                const connectBtn = document.getElementById('connectBtn');
                
                if (!apiKey) {
                    this.showNotification('API 키를 입력해주세요', 'error');
                    return;
                }
                
                connectBtn.textContent = '연결 중...';
                connectBtn.disabled = true;
                
                try {
                    // API 키 테스트 (시뮬레이션)
                    await new Promise(resolve => setTimeout(resolve, 2000));
                    
                    if (apiKey.startsWith('sk-ant-') || apiKey.length > 10) {
                        this.apiKey = apiKey;
                        this.isConnected = true;
                        localStorage.setItem('claudeApiKey', apiKey);
                        
                        document.getElementById('statusDot').classList.add('connected');
                        connectBtn.textContent = '연결됨 ✓';
                        connectBtn.classList.add('connected');
                        
                        this.showNotification('🎉 API 연결 성공!');
                    } else {
                        throw new Error('올바른 API 키를 입력해주세요');
                    }
                } catch (error) {
                    this.showNotification('API 연결 실패: ' + error.message, 'error');
                    connectBtn.textContent = '연결하기';
                } finally {
                    connectBtn.disabled = false;
                }
            }
            
            async callKokiAPI(message) {
                if (!this.isConnected) {
                    // 데모 모드 응답
                    return new Promise((resolve) => {
                        setTimeout(() => {
                            const responses = [
                                `"${message}"에 대한 Koki의 답변입니다! 실제 API 연결 시 더 정확한 답변을 받을 수 있습니다. 🤖`,
                                `흥미로운 질문이네요! "${message}"에 대해 더 자세히 알아보시겠어요?`,
                                `좋은 질문입니다! Koki가 "${message}"에 대해 도움을 드리겠습니다.`,
                                `"${message}"... 정말 좋은 주제네요! 설정에서 API를 연결하면 더 정확한 답변을 받을 수 있어요.`,
                                `듀얼 AI 모드로 Claude와 GPT-4의 답변을 비교해보시는 건 어떨까요? 🤖`
                            ];
                            resolve(responses[Math.floor(Math.random() * responses.length)]);
                        }, 1000 + Math.random() * 2000);
                    });
                }
                
                // 실제 API 호출 시뮬레이션
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(`[실제 Claude API 응답] "${message}"에 대한 정확하고 상세한 답변입니다. API가 정상적으로 연결되어 작동하고 있습니다.`);
                    }, 1500);
                });
            }
            
            async processDualAI() {
                const question = document.getElementById('dualQuestion').value.trim();
                const processBtn = document.getElementById('processBtn');
                
                if (!question) {
                    this.showNotification('질문을 입력해주세요', 'error');
                    return;
                }
                
                processBtn.textContent = '🔄 처리 중...';
                processBtn.disabled = true;
                
                try {
                    const [claudeResponse, gptResponse] = await Promise.all([
                        this.getClaudeResponse(question),
                        this.getGPTResponse(question)
                    ]);
                    
                    this.dualResponses.claude = claudeResponse;
                    this.dualResponses.gpt = gptResponse;
                    this.dualResponses.integrated = this.generateIntegratedResponse(claudeResponse, gptResponse);
                    
                    this.showDualResponse();
                    this.showNotification('🤖 듀얼 AI 응답 완성!');
                    
                } catch (error) {
                    this.showNotification('AI 응답 생성 실패: ' + error.message, 'error');
                } finally {
                    processBtn.textContent = '🚀 AI 응답 생성';
                    processBtn.disabled = false;
                }
            }
            
            async getClaudeResponse(question) {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(`[Claude 응답]\n\n"${question}"에 대한 Claude의 상세하고 체계적인 분석입니다.\n\n• 핵심 포인트를 정확히 파악\n• 논리적이고 구조화된 설명\n• 실용적인 해결방안 제시\n\nClaude는 정확성과 깊이 있는 사고를 바탕으로 답변합니다.`);
                    }, 1500);
                });
            }
            
            async getGPTResponse(question) {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(`[GPT-4 응답]\n\n"${question}"에 대한 GPT-4의 창의적이고 논리적인 접근입니다.\n\n• 다각도 분석과 창의적 사고\n• 혁신적인 아이디어 제시\n• 실제 적용 가능한 솔루션\n\nGPT-4는 폭넓은 지식과 추론 능력으로 답변합니다.`);
                    }, 1800);
                });
            }
            
            generateIntegratedResponse(claude, gpt) {
                return `[통합 답변 🎯]\n\nClaude와 GPT-4의 응답을 종합한 최적화된 결과입니다:\n\n✅ 공통 견해:\n두 AI 모두 해당 질문의 중요성을 인정하며, 체계적 접근이 필요함을 강조합니다.\n\n🔍 Claude의 강점:\n• 정확하고 체계적인 분석\n• 논리적 구조화\n• 신뢰할 수 있는 정보\n\n🎯 GPT-4의 강점:\n• 창의적이고 혁신적 사고\n• 다각도 분석\n• 실용적 솔루션\n\n💡 최종 결론:\n두 AI의 장점을 결합하여 더 완전하고 실용적인 답변을 제시합니다.`;
            }
            
            switchDualTab(tabName) {
                document.querySelectorAll('.ai-tab').forEach(tab => tab.classList.remove('active'));
                document.querySelector(`[data-tab="${tabName}"]`).classList.add('active');
                this.currentDualTab = tabName;
                this.showDualResponse();
            }
            
            showDualResponse() {
                const responseArea = document.getElementById('dualResponse');
                const response = this.dualResponses[this.currentDualTab];
                
                if (response) {
                    responseArea.textContent = response;
                } else {
                    const tabNames = {
                        claude: 'Claude',
                        gpt: 'GPT-4', 
                        integrated: '통합'
                    };
                    responseArea.textContent = `${tabNames[this.currentDualTab]} 응답이 아직 생성되지 않았습니다.`;
                }
            }
            
            async initVideoPage() {
                try {
                    this.localStream = await navigator.mediaDevices.getUserMedia({
                        video: { facingMode: 'user' },
                        audio: true
                    });
                    
                    const video = document.getElementById('localVideo');
                    video.srcObject = this.localStream;
                    video.style.display = 'block';
                    document.querySelector('.video-placeholder').style.display = 'none';
                    
                    this.isCameraOn = true;
                    this.isMicOn = true;
                    this.updateVideoControls();
                    
                    this.showNotification('📹 카메라가 활성화되었습니다!');
                } catch (error) {
                    this.showNotification('📹 카메라 접근 권한이 필요합니다', 'warning');
                }
            }
            
            toggleCamera() {
                if (this.localStream) {
                    const videoTrack = this.localStream.getVideoTracks()[0];
                    if (videoTrack) {
                        videoTrack.enabled = !videoTrack.enabled;
                        this.isCameraOn = videoTrack.enabled;
                        this.updateVideoControls();
                        this.showNotification(this.isCameraOn ? '📹 카메라 켜짐' : '📹 카메라 꺼짐');
                    }
                } else {
                    this.initVideoPage();
                }
            }
            
            toggleMicrophone() {
                if (this.localStream) {
                    const audioTrack = this.localStream.getAudioTracks()[0];
                    if (audioTrack) {
                        audioTrack.enabled = !audioTrack.enabled;
                        this.isMicOn = audioTrack.enabled;
                        this.updateVideoControls();
                        this.showNotification(this.isMicOn ? '🎤 마이크 켜짐' : '🎤 마이크 꺼짐');
                    }
                } else {
                    this.initVideoPage();
                }
            }
            
            endVideoCall() {
                if (this.localStream) {
                    this.localStream.getTracks().forEach(track => track.stop());
                    this.localStream = null;
                    
                    document.getElementById('localVideo').style.display = 'none';
                    document.querySelector('.video-placeholder').style.display = 'block';
                    
                    this.isCameraOn = false;
                    this.isMicOn = false;
                    this.updateVideoControls();
                    
                    this.showNotification('📞 영상 통화가 종료되었습니다');
                }
            }
            
            updateVideoControls() {
                const cameraBtn = document.getElementById('cameraBtn');
                const micBtn = document.getElementById('micBtn');
                
                cameraBtn.classList.toggle('active', this.isCameraOn);
                micBtn.classList.toggle('active', this.isMicOn);
            }
            
            openTool(toolName) {
                const tools = {
                    writer: '✍️ 문서 작성 도구입니다. AI가 전문적인 문서를 작성해드립니다!',
                    translator: '🌐 번역 도구입니다. 다양한 언어로 정확하게 번역해드립니다!',
                    coder: '💻 코드 생성 도구입니다. 원하는 기능의 코드를 자동 생성합니다!',
                    analyzer: '📊 분석 도구입니다. 데이터를 분석하고 인사이트를 제공합니다!',
                    summarizer: '📝 요약 도구입니다. 긴 글을 핵심만 간추려 요약합니다!',
                    tutor: '🎓 학습 도우미입니다. AI 개인교사가 학습을 도와드립니다!'
                };
                
                this.showNotification(tools[toolName] || '도구를 준비 중입니다...');
                
                // 채팅 페이지로 이동하여 해당 도구 사용 안내
                this.switchPage('chatPage');
                setTimeout(() => {
                    this.addMessage(`${tools[toolName]} 어떻게 도와드릴까요?`, 'assistant');
                }, 500);
            }
            
            speakText(text) {
                if (!this.settings.ttsEnabled || !this.synthesis) return;
                
                this.synthesis.cancel();
                const utterance = new SpeechSynthesisUtterance(text);
                utterance.lang = 'ko-KR';
                utterance.rate = 0.9;
                utterance.pitch = 1.0;
                
                const voices = this.synthesis.getVoices();
                const koreanVoice = voices.find(voice => voice.lang.startsWith('ko'));
                if (koreanVoice) {
                    utterance.voice = koreanVoice;
                }
                
                this.synthesis.speak(utterance);
            }
            
            loadSettings() {
                const saved = localStorage.getItem('kokiSettings');
                if (saved) {
                    this.settings = { ...this.settings, ...JSON.parse(saved) };
                }
                
                document.getElementById('speechToggle').checked = this.settings.speechEnabled;
                document.getElementById('ttsToggle').checked = this.settings.ttsEnabled;
            }
            
            saveSettings() {
                localStorage.setItem('kokiSettings', JSON.stringify(this.settings));
            }
            
            loadApiKey() {
                const saved = localStorage.getItem('claudeApiKey');
                if (saved) {
                    document.getElementById('claudeApiKey').value = saved;
                    // 자동 연결 시도
                    if (saved.length > 10) {
                        setTimeout(() => this.connectAPI(), 1000);
                    }
                }
            }
            
            updateTime() {
                function updateClock() {
                    const now = new Date();
                    const timeString = now.toLocaleTimeString('ko-KR', { 
                        hour: '2-digit', 
                        minute: '2-digit',
                        hour12: false 
                    });
                    document.getElementById('currentTime').textContent = timeString;
                }
                
                updateClock();
                setInterval(updateClock, 1000);
            }
            
            showNotification(message, type = 'success') {
                const notification = document.createElement('div');
                notification.className = `notification ${type}`;
                notification.textContent = message;
                
                document.body.appendChild(notification);
                
                setTimeout(() => notification.classList.add('show'), 100);
                setTimeout(() => {
                    notification.classList.remove('show');
                    setTimeout(() => notification.remove(), 300);
                }, 3000);
            }
        }
        
        // 전역 함수
        function clearCache() {
            localStorage.clear();
            sessionStorage.clear();
            window.kokiApp.showNotification('🔄 캐시가 지워졌습니다!');
            setTimeout(() => location.reload(), 1500);
        }
        
        // 앱 초기화
        document.addEventListener('DOMContentLoaded', () => {
            window.kokiApp = new KokiMobileApp();
        });
    </script>
</body>
</html>
